<%= render 'layouts/welcome_header' %>

<div class="banner">
  <!-- Carousel
  ================================================== -->
  <div id="myCarousel" class="carousel slide" data-ride="carousel">
    <!-- Indicators -->

    <div class="carousel-inner">
      <div class="item active">
        <img src="/assets/s-banner-2.png" alt="Banner">
        <div class="container">
          <div class="carousel-caption">
            <h1>程序员的空间</h1>

          </div>
        </div>
      </div>
    </div>
  </div><!-- /.carousel -->
</div>

<div class="container">
  <h2 class="text-center">与更多程序员／工程师在一起的工作空间</h2>
  <br>
  <p>
    占位<del>（艺术/设计行业的人员整体偏感性，比较在意生活质量和工作环境（比如干净宽敞的空间，完善的设备等），因为生活是设计灵感的来源，
    因此一个好的工作环境会更容易让设计师产生好的创意。）</del>
  </p>

  <div class="main main-list tab-content">
    <div class="row placeholders">
      <% @spaces.each do |space| %>
        <div class="col-md-3">
          <div class="thumbnail">
            <% if space.photos.empty? %>
              <%= image_tag '/assets/list1.jpg', class: 'index-photo' %>
            <% else %>
              <%= image_tag space.photos.first.data.url(:thumb), class: 'index-photo' %>
            <% end %>
            <%#= image_tag '/assets/list1.jpg' %>
            <div class="caption">
              <h5><%= link_to space.title, space %></h5>
              <p><%= space.space_setting.content %> | <%= space.city.content %> <strong class="text-right"><%= space.price_month %>每月</strong></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

</div> <!-- /container -->


<%= render 'layouts/footer' %>